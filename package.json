{
    "name": "acliv",
    "private": true,
    "version": "1.0.7",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "tauri": "tauri",
        "web:start": "npm run build && cargo run --manifest-path src-tauri/Cargo.toml --no-default-features --features web --bin acliv-web",
        "web:build": "npm run build && cargo build --release --manifest-path src-tauri/Cargo.toml --no-default-features --features web --bin acliv-web"
    },
    "devDependencies": {
        "@sveltejs/vite-plugin-svelte": "^5.1.1",
        "@tauri-apps/cli": "^2.10.1",
        "@types/dompurify": "^3.0.5",
        "svelte": "^5.55.3",
        "svelte-check": "^4.4.6",
        "typescript": "^5.9.3",
        "vite": "^6.4.2"
    },
    "dependencies": {
        "@tauri-apps/api": "^2.10.1",
        "dompurify": "^3.3.3",
        "highlight.js": "^11.9.0",
        "marked": "^12.0.0",
        "mermaid": "^11.14.0"
    }
}
